- Scanner in deadloop on input 'let y = 2 in "sin on y;'
- fix imports and type hinting
- Implement better syntax warnings
- Rewrite the scanner
- Implement map, fold and filter
- debug interpreter on (lambda x . lambda y . x + y) on 1, 2
- implement function application by argument juxtaposition
- debug
    let a = let b = 2 in b in c
    let b = 2 in b
- clean parser code, expecially parsing of binary expression: avoid the restore left associativity function
- implement memoization
# fix free variables for "let fac = \n . if n == 1 then 1 else fac(n - 1) in fac(4)"
